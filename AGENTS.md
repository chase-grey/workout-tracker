# Git publishing

The user approved `https://github.com/chase-grey/workout-tracker.git` as a destination for this project's source code and authorized future ordinary pushes without another confirmation on 2026-09-17.

When publishing completed work, use this approval without asking again. For the saved Codex command rule, use:

```powershell
git -C C:/EpicSource/Projects/workout-tracker push https://github.com/chase-grey/workout-tracker.git main
```

This standing approval covers normal pushes to this repository. Force pushes, branch deletion, and publishing to other repositories require separate authorization. Follow any higher-priority managed permission requirements.
